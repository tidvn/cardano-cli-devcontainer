{
  "name": "Cardano CLI Devcontainer",
  "image": "ghcr.io/blinklabs-io/cardano-node:main",
  "features": {
    "docker-in-docker": "latest"
  },
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspaces,type=bind"
    
  ],
  "postCreateCommand": "echo 'Container started'",
  "postAttachCommand":"cardano-node run --config /opt/cardano/preprod/config.json --database-path /opt/cardano/preprod/db/ --socket-path /opt/cardano/preprod/db/node.socket --host-addr 127.0.0.1 --port 3001 --topology /opt/cardano/preprod/topology.json",
  "runArgs": [
    "--env","NETWORK=preprod",
    "--publish","3001:3001",  // "--volume",
    // ".node/data:/data/db",
    // "--volume",
    // ".node/ipc:/ipc",
    "--cap-add=SYS_NICE",
    "--network=host"
  ],
  "shutdownAction": "none"
}